package Ball;

public class Ball {
  // Variables
  private double x;
  private double y;

  // Constructor (overloaded)
  public Ball() {
    x = 0.0;
    y = 0.0;
  }
  public Ball(double newX, double newY) {
    x = newX;
    y = newY;
  }

  // Methods
}
